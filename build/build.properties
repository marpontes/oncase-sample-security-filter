app.name=oncase-sample-security-filter
prj.version=0.1.0
src.dir=src
classes.dir=classes
lib.dir=lib
dist.dir=dist
jar.name=${app.name}-${prj.version}.jar
jar.file=${dist.dir}/${jar.name}
test.file=/opt/curitiba2/biserver-ce/tomcat/webapps/pentaho/WEB-INF/lib/${jar.name}
#test.file2=/opt/curitiba/biser/lib/${jar.name}
